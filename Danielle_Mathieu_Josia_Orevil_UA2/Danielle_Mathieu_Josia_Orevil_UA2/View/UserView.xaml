<UserControl x:Class="Danielle_Mathieu_Josia_Orevil_UA2.View.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Danielle_Mathieu_Josia_Orevil_UA2.View"
              xmlns:vm="clr-namespace:Danielle_Mathieu_Josia_Orevil_UA2.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:UserViewModel/>
    </UserControl.DataContext>
    <Grid Background="#E8F0FE">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="159*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel HorizontalAlignment="Left" Width="350" Margin="225,0,0,0">

            <TextBlock Text="Gestion des Utilisateurs"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"/>



            <TextBlock Text="Nom:" />
            <TextBox Text="{Binding SelectedUser.Nom}" />

            <TextBlock Text="Prénom:" />
            <TextBox Text="{Binding SelectedUser.Prenom}" />

            <TextBlock Text="Nom d'utilisateur:" />
            <TextBox Text="{Binding SelectedUser.UserName}" />

            <TextBlock Text="Mot de passe:" />
            <TextBox Text="{Binding SelectedUser.UserPassword}" />

            <TextBlock Text="Email:" />
            <TextBox Text="{Binding SelectedUser.Email}" />

            <TextBlock Text="ID Rôle:" />
            <TextBox Text="{Binding SelectedUser.IdRole}" />


            <ListBox ItemsSource="{Binding Users}"
            SelectedItem="{Binding SelectedUser}"
            DisplayMemberPath="UserName"
            Height="111" Width="347"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding AddUserCommand}" Content="Ajouter" Width="100"/>
                <Button Command="{Binding UpdateUserCommand}" Content="Modifier" Width="100"/>
                <Button Command="{Binding DeleteUserCommand}" Content="Supprimer" Width="100"/>
            </StackPanel>


        </StackPanel>
    </Grid>

</UserControl>
