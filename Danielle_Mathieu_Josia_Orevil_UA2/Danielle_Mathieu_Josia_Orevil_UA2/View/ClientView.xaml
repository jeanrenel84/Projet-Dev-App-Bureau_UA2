<UserControl x:Class="Danielle_Mathieu_Josia_Orevil_UA2.View.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Danielle_Mathieu_Josia_Orevil_UA2.View"
             xmlns:vm="clr-namespace:Danielle_Mathieu_Josia_Orevil_UA2.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ClientViewModel/>
    </UserControl.DataContext>

    <Grid Background="#E8F0FE">
        <StackPanel HorizontalAlignment="Center" Width="350">

            <TextBlock Text="Gestion des Clients"
                       FontSize="20"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <TextBlock Text="Nom :" />
            <TextBox Text="{Binding SelectedClient.nom, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Prénom :" />
            <TextBox Text="{Binding SelectedClient.prenom, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Email :" />
            <TextBox Text="{Binding SelectedClient.email, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Téléphone :" />
            <TextBox Text="{Binding SelectedClient.telephone, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Adresse :" />
            <TextBox Text="{Binding SelectedClient.adresse, UpdateSourceTrigger=PropertyChanged}" />

            <ListBox ItemsSource="{Binding Clients}"
                     SelectedItem="{Binding SelectedClient}"
                     DisplayMemberPath="nom"
                     Height="100"
                     Margin="0,10,0,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Command="{Binding AddClientCommand}" Content="Ajouter" Width="100" Margin="5,0"/>
                <Button Command="{Binding UpdateClientCommand}" Content="Modifier" Width="100" Margin="5,0"/>
                <Button Command="{Binding DeleteClientCommand}" Content="Supprimer" Width="100" Margin="5,0"/>
            </StackPanel>

        </StackPanel>
    </Grid>

</UserControl>
